#include <stdio.h>
/**
 * main - Prints number of base 16 in lowercase
 *
 * Return: Always (0)
 */

int main(void)
{
int num;
for (num = 0, num <=15, num ++)
{
putchar(num);
;
}
putchar('\n');
return (0);
}
